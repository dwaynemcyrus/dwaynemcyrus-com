# Canonical Allowlist (Phase 1)

Only documents whose `canonical` path matches one of these prefixes are eligible for public export.

## Allowed Prefixes
- `/lab/`
- `/projects/`

Update this list before expanding public surfaces.
