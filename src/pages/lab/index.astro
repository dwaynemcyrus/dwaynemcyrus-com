---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const entries = await getCollection('lab');
---
<BaseLayout title="Sovereign Labs — Experimental Systems">
	<main slot="main">
		<h1>Sovereign Labs — Experimental Systems</h1>

		<p>
			Sovereign Labs is where I test the boundaries of applied AI — running experiments in memory, behavior,
			writing, and decision architecture. Each lab represents a question I’m trying to answer while building
			MirrorOS and its surrounding ecosystem.
		</p>

		<p>
			These are not apps.<br />
			They are <strong>experiments in cognition.</strong>
		</p>

		<p>Each lab follows a simple methodology:</p>

		<ul>
			<li>Form a hypothesis</li>
			<li>Build a prototype</li>
			<li>Test under real constraints</li>
			<li>Evaluate what the model understood</li>
			<li>Document what I learned</li>
			<li>Fold the insight back into the larger system</li>
		</ul>

		{entries.length === 0 ? (
			<p>No public lab entries yet.</p>
		) : (
			<ul>
				{entries.map((entry) => (
					<li>
						<a href={`/lab/${entry.id}`}>{entry.data.title}</a>
					</li>
				))}
			</ul>
		)}
	</main>
</BaseLayout>
